{
  "name": "facdoc-backend",
  "version": "1.0.0",
  "description": "부동산 매물 정보 관리 시스템 백엔드",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:frontend": "cd ../frontend && npm install && npm run build && mkdir -p ../backend/frontend && cp -r build ../backend/frontend",
    "postinstall": "npm run build:frontend",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "real-estate",
    "property",
    "management"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
